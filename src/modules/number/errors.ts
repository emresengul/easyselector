export enum RandomNumberErrors {
    SELECT_VALUE_MUST_BE_LESS_THAN_MAX_VALUE = "Select value must be less than max value",
    SELECT_VALUE_MORE_THAN_ARRAY_LENGTH = "You can't choose total select value more than array length"
}