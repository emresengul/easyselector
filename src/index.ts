// export * from "./censor"
// RandomArray.random(['a', 'b', 'c'],{unique:true})});
// RandomColor.random();
// RandomBoolean.random()
// const { randomselector } = require("./");
// randomselector.array.random(['a','b'],{unique:true})
// randomselector.color.random()
// randomselector.boolean.random()
// randomselector.number.random(1,999,{select:25})

import { RandomSelector } from "./randomselector";


export const randomSelector: RandomSelector = new RandomSelector();